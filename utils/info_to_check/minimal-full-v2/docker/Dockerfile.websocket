# Use PHP CLI for running WebSocket server
FROM php:8.0-cli

# Set working directory
WORKDIR /var/www/html

# Copy WebSocket server file
COPY ../../backend/websocket.php /var/www/html/websocket.php

# Expose WebSocket server port
EXPOSE 8081

# Run WebSocket server
CMD ["php", "websocket.php"]
