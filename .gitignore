
# ======================
# Node.js & Fastify
# ======================
node_modules/    # Dependencies installed via npm or yarn
.dist/           # Compiled and minified files
build/           # Build artifacts generated for deployment

# ======================
# Environment Variables
# ======================
.env             # Environment variables file (contains secrets and API keys)

# ======================
# IDE and Editor Configs
# ======================
.vscode/         # VSCode editor settings and extensions
.DS_Store        # macOS system file (not needed in repositories)

# ======================
# Logs and Debugging
# ======================
logs/            # Logs folder for application logs
npm-debug.log    # Log file generated when npm encounters an issue
yarn-error.log   # Log file generated when yarn encounters an issue

# ======================
# SQLite Database
# ======================
sqlite_data/     # Folder for SQLite database storage
*.sqlite         # SQLite database files
*.sqlite3        # SQLite v3 database files
*.db             # Generic database files
*.db-wal         # Write-ahead log for SQLite databases
*.db-shm         # Shared memory files for SQLite

# ======================
# Docker
# ======================
.dockerignore                   # Docker ignore file (similar to .gitignore but for Docker)
docker-compose.override.yml     # Local override for docker-compose setup
**/docker-entrypoint-initdb.d/  # Init scripts for database setup in Docker

# ======================
# Tailwind CSS & Frontend Build
# ======================
.next/           # Next.js build directory
out/             # Static export directory for Next.js
*.cache/         # Caches generated for faster builds
*.lock           # Lock files for package managers (yarn.lock, package-lock.json)

# ======================
# Babylon.js / WebGL Artifacts
# ======================
*.glb            # Binary GLTF files for 3D assets
*.gltf           # GLTF 3D model files
*.bin            # Binary files used with GLTF
*.babylon        # Babylon.js-specific 3D model format

# ======================
# Temporary & Local Files
# ======================
_local/          # Local folder for personal notes, tests, or temp files
tmp*             # Temporary files (wildcard to match any file prefixed with tmp)
